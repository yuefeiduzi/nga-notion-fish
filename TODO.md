# TODO - NGA 阅读器

进度与下一步。**当前状态：可以日常使用** —— 首页 / 板块页 / 帖子页三类页面，解析、排版、图片、表情
都已在登录态真机上跑通（2026-09）。

## 现在这一步：边用边补

日常用：把 `extension/` 装进 Chrome（[README · 安装](README.md#安装)）。
哪里不对就点侧边栏「更多 → 复制诊断信息」，把那段文本发出来（比截图有用）。

### 改动后的快速自检（10 分钟，改代码或 NGA 改版后过一遍）

| # | 看什么 | 预期 |
| --- | --- | --- |
| 1 | 首页 `/` | 板块按分组平铺，悬停能 ☆ 收藏，点进板块 |
| 2 | 列表页 `/thread.php?fid=x` | 每行：编号 + 标签 + 标题 + 作者/时间/回复/浏览 |
| 3 | 帖子页 `/read.php?tid=x` | 楼层编号 `01 02 03`、楼主标记、时间、赞数 |
| 4 | 引用与嵌套引用 | 左侧青色竖线的引用块，头部保留「Post by 某人 (时间)」 |
| 5 | 图片 | 默认是「图片（点击加载）」占位；点「显示图片」能整页加载；**宽度不超过正文栏** |
| 6 | 表情 | 按原图大小**行内**排（不是块级大图），无图模式下也照常显示 |
| 7 | 折叠块 `[collapse]` | 显示成可展开的 `<details>`，内容不丢 |
| 8 | 翻页 / 后退 | 底部胶囊按钮；点下一页不整页刷新；浏览器后退内容跟着变 |
| 9 | 暗色 | 侧边栏底部三段开关切「深色」，正文与引用块都能看 |
| 10 | 应急伪装 | 连按两下 `Esc` 出现假页面，再按两下切回 |
| 11 | popup | 点扩展图标，开关/主题改动即时生效（不用刷新页面） |
| 12 | 别的页面 | 搜索页/用户页等不支持路径应**保持原站**，不能盖上一层空壳 |

### 出错时的三步（按这个顺序）

1. **先拿到现场**：侧边栏「更多 → 复制诊断信息」，或兜底页上的「复制诊断」
2. **临时绕过**：popup 关掉总开关；或在地址栏 URL 后面加 `?ngr=off`（本次会话都不再接管）
3. **白屏**：说明 `ngr-pending` 没被摘掉，把 DevTools Console 第一条红字发我

### 改代码时

```bash
git log --oneline -5         # 上次干到哪
cat AGENTS.md                # 架构约定、分层职责、踩过的坑
cat dev/nga-dom-notes.md     # 真实结构 + 出处（改选择器必看）
python3 dev/server.py 8765   # 只改代码时才需要：样例页跑通再去真机
```

样例页能验证排版、解析与交互；**选择器与站点行为一律以登录态真机为准**。

## 已完成 ✓

### 架构（v0.3 → 0.5，Chrome MV3 扩展）
- [x] 从 Tampermonkey 用户脚本迁移到 Chrome MV3 扩展（零构建、原生 ESM），旧用户脚本已从仓库移除（历史在 git 里）
- [x] 不再「原站上盖一层」：`document_start` 藏住原站，只把它当数据源
- [x] 分层：`parse.js`（Document → 模型）/ `sanitize.js`（正文净化）/ `view/*`（渲染）/ `app.css`（设计 token）
- [x] 站内跳转：先做过 `fetch + 自渲染`，真机实测发现首页/帖子页正文是 JS 注入的，
      已改回整页导航（原站仍被藏住，所以没有闪动）
- [x] 失败兜底：解析失败/未登录/加载失败都有可操作的提示页，且能一键回原站
- [x] 一键诊断：`core/diagnose.js` 导出选择器命中数 + 抽取字段 + 首楼 HTML（侧边栏与兜底页都有入口）
- [x] 不支持的页面（搜索/用户页等）保持原站，不接管
- [x] 本地调试器：`dev/server.py` + 结构仿真的假 NGA 页面（无需登录即可调 UI）

### 视觉（参考 aihot.news 日报）
- [x] 暖纸底 + 细线分隔 + 数字编号的阅读版式
- [x] 侧边栏：品牌 / 内容 / 板块（收藏）/ 最近 / 更多，含三段式主题开关
- [x] 阅读页：kicker（板块 + 页码）、32px 大标题、2px 分隔线、楼层 `01/02/…` 编号
- [x] 列表页：编号 + 标签 + 标题 + 作者/时间/回复/浏览
- [x] 首页：板块分组网格 + ☆ 收藏
- [x] 深色 / 跟随系统 / 浅色三档主题
- [x] 无图模式（占位按钮 + 单张/整页加载）
- [x] 应急伪装：中性图标与中性标签页标题、连按两下 Esc 的假页面

### 内容保真
- [x] 引用块（含嵌套引用）转 `<blockquote>` 并保留作者/时间/链接
- [x] 折叠块 `[collapse]` → `<details>`
- [x] 表格、代码块、列表
- [x] 表情单独识别（`class` 含 `smile` / 地址含 `/post/smile/`）：按原图大小行内排、不进预览、无图模式下也照常显示
- [x] 图片：`data-src` / `data-srclazy` 还原、宽度贴齐正文栏、1px 装饰图过滤、点击全屏预览
- [x] 外链 `target=_blank`、内联脚本/样式/广告/签名过滤

### 真机核对（2026-09，登录态 Chrome ✅）
- [x] 首页 / 列表页 / 帖子页三类页面跑通（411 个板块、47 个主题、楼层正文与引用都正确）
- [x] 站点数据两条路径：`commonui.postArg.data`（含 `postTime` unix / `recommend`）与选择器兜底
- [x] 时间字段：`span#postdate{N}` + 站点数据 `postTime`；赞同数：站点数据 `recommend`
- [x] 列表页列义：回复数在 `td.c1 a.replies`，`td.c4` 是最后回复（时间 + 人）
- [x] GBK 编码确认（HTTP 头是 GBK）；错误页 `ERROR:5/15/1`
- [x] 结论：fetch 拿不到首页/帖子页正文 → 整页导航 + content script 重新接管
- [x] 图片懒加载：`nga/lazy-images.js` 临时把原站切成「有布局但看不见」+ 逐张 `scrollIntoView` 催 `src`
- [x] 大图宽度：原站 JS（`ubbcode.adjImgSize`）会往我们克隆的图上写内联 `max-width:982px`
      （比正文栏宽 300px），已用「图片规则 `!important` + 摘掉克隆图上的原站 `data-*` 钩子」压住
- [x] 表情与正式图片区分（原图 69×60、没有尺寸属性，只能看类名/地址）

### 维护（2026-09）
- [x] 扩展改名为 **NGA 阅读器**（popup、侧边栏品牌、文档同步；标签页标题仍是可改的中性名）
- [x] 清掉旧用户脚本存档与一批死代码（未使用的 CSS 类、只在本文件使用的导出）

## 待办

### 内容与解析
- [ ] 楼层号：目前按 20 楼/页推算，还没在第 N 页（非首页）上核对过
- [ ] `postBtnPos` 里的赞踩按钮是 JS 后填的，真机上常为空（赞数改用站点数据 `recommend`）
- [ ] 引用块里的图拿不到地址（原站把它裁掉了），现在是「点击加载」占位点了没反应
- [ ] 贴条（`.comment_c_*`）目前整块丢弃，考虑做成「查看贴条」折叠
- [ ] 折叠块 `.collapse_content` 内容为空（点击才去原站拉），现在是提示文案，可考虑「展开即回原站」
- [ ] 表情 alt（如 `咦`）现在是图片加载失败的兜底，可以考虑失败时直接显示 `[咦]` 文字

### 体验
- [ ] 楼层锚点跳转（`#pid` / 跳楼输入框）与「本页楼层目录」
- [ ] 收藏板块支持分组排序、拖拽调整
- [ ] 阅读进度记忆（记住上次读到的楼层）
- [ ] 帖子内搜索 / 只看有图楼层

### 工程
- [ ] `parse.js` 的 fixture 快照测试（保存真实 HTML 样本，回归时对比模型输出）
- [ ] 扩展图标（目前是默认图标；伪装用的中性图标已内联在 `app.js`）
- [ ] 打包 zip 供离线安装 / 上架 Edge Add-ons
- [ ] 定期跑 `bash dev/reference/refresh.sh` 看上游选择器有无变化（改版预警）

### 远期
- [ ] 支持搜索页、用户页、收藏页的自渲染
- [ ] 多站点适配（其它论坛/资讯站共用同一套阅读外壳）
- [ ] 键盘流：`n/p` 翻页、楼层间 `J/K` 跳转

## 技术笔记

### 页面判断
- `/` → 首页；`/thread.php?fid=` → 板块页；`/read.php?tid=` → 帖子页；其它 → 保持原站
- 访客被拦：标题为「未登录」/「访客不能直接访问」，正文含 `ERROR:1` / `ERROR:15`

### NGA 自带数据（当前页面才有）
- `window.commonui.postArg.data[i]`：`pid` / `pAid` / `i`（楼层序号）/ `contentC` / `subjectC` / `uInfoC`
- `window.commonui.topicArg.data[i]`：数组，`[1]` 标题元素、`[2]` 作者元素、`[7]` fid、`[8]` tid
- `window.__PAGE = [url, 总页数, 当前页, …]`
- 加 `?nopostarg=1` 打开样例页可强制走选择器兜底路径

### 图片与表情（都是真机实测，详见 `dev/nga-dom-notes.md`）
- 原站 JS（`ubbcode.adjImgSize`）**会改写我们渲染出来的图**：写内联 `max-width:<原站布局宽度>px`
  （实测 982~1030）与主题色描边 ⇒ 排版规则必须 `!important`，克隆图要摘掉 `data-argi`/`data-nw`/… 钩子
- 表情是 `<img class="smile_ac" src="…/post/smile/ac11.png" alt="咦">`：没有尺寸属性、原图 69×60，
  只能靠 `class` 含 `smile` 或地址含 `/post/smile/` 认（不能按「小尺寸」判）
- 真图要滚进视口才把地址写进 `src`，所以「显示图片」要先 `nga/lazy-images.js` 唤醒

### 存储
- `chrome.storage.local` 单键 `reader-settings`：`enabled / theme / hideImages / stealth / bossKey / fontScale / brandText / favorites / recents`
- `brandText` = 应急伪装时的标签页标题（默认 `阅读器`）；侧边栏品牌名是固定值（`NGA 阅读器`）
- 本地调试时 `dev/harness-boot.js` 提供 localStorage 版模拟

### 快捷键
- `Esc Esc` 假页面 · `i` 无图 · `t` 主题 · `g` 首页 · `r` 刷新 · `j/k` 滚动

### 可访问性/摸鱼相关
- `sessionStorage['ngr-skip']`：本次会话跳过接管（用于「以原站方式打开」）
- URL 上加 `?ngr=off` 也会跳过接管
